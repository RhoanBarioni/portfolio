import { jsx } from "react/jsx-runtime";
import { motion, useReducedMotion } from "motion/react";
//#region src/components/ui-kit/reveal.tsx
function Reveal({ children, delay = 0, y = 16, className, as = "div" }) {
	const reduced = useReducedMotion();
	const MotionTag = motion[as];
	if (reduced) return /* @__PURE__ */ jsx(as, {
		className,
		children
	});
	return /* @__PURE__ */ jsx(MotionTag, {
		className,
		initial: {
			opacity: 0,
			y
		},
		whileInView: {
			opacity: 1,
			y: 0
		},
		viewport: {
			once: true,
			margin: "-60px"
		},
		transition: {
			duration: .55,
			delay,
			ease: [
				.22,
				1,
				.36,
				1
			]
		},
		children
	});
}
//#endregion
export { Reveal as t };
