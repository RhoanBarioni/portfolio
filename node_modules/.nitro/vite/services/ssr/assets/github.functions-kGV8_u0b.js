import { d as TSS_SERVER_FUNCTION, t as createServerFn } from "./createServerFn-CIHAFgYl.js";
//#region node_modules/@tanstack/start-server-core/dist/esm/createServerRpc.js
var createServerRpc = (serverFnMeta, splitImportFn) => {
	const url = "/_serverFn/" + serverFnMeta.id;
	return Object.assign(splitImportFn, {
		url,
		serverFnMeta,
		[TSS_SERVER_FUNCTION]: true
	});
};
//#endregion
//#region src/lib/github.functions.ts?tss-serverfn-split
var EMPTY = {
	available: false,
	publicRepos: 0,
	totalStars: 0,
	totalForks: 0,
	pinned: [],
	latest: [],
	repos: [],
	languages: [],
	activity: [],
	commits: [],
	days: []
};
function describeEvent(event) {
	switch (event.type) {
		case "PushEvent": {
			const count = event.payload?.commits?.length ?? 0;
			return `Pushed ${count} commit${count === 1 ? "" : "s"}`;
		}
		case "CreateEvent": return `Created ${event.payload?.ref_type ?? "resource"}`;
		case "PullRequestEvent": return `${event.payload?.action === "closed" ? "Closed" : "Opened"} a pull request`;
		case "IssuesEvent": return `${event.payload?.action === "closed" ? "Closed" : "Opened"} an issue`;
		case "WatchEvent": return "Starred a repository";
		case "ForkEvent": return "Forked a repository";
		case "ReleaseEvent": return "Published a release";
		default: return event.type.replace(/Event$/, "");
	}
}
var DAYS = 119;
function buildDays(events) {
	const counts = /* @__PURE__ */ new Map();
	for (const event of events) {
		const day = event.created_at.slice(0, 10);
		const weight = event.type === "PushEvent" ? event.payload?.commits?.length ?? 1 : 1;
		counts.set(day, (counts.get(day) ?? 0) + weight);
	}
	const today = /* @__PURE__ */ new Date();
	const days = [];
	for (let i = DAYS; i >= 0; i -= 1) {
		const d = new Date(Date.UTC(today.getUTCFullYear(), today.getUTCMonth(), today.getUTCDate()));
		d.setUTCDate(d.getUTCDate() - i);
		const key = d.toISOString().slice(0, 10);
		days.push({
			date: key,
			count: counts.get(key) ?? 0
		});
	}
	return days;
}
var getGithubStats_createServerFn_handler = createServerRpc({
	id: "f5ddb5f2df9ced8daed0f40b243f2db2e93dab13b2afcba43ad3b69cb0005943",
	name: "getGithubStats",
	filename: "src/lib/github.functions.ts"
}, (opts) => getGithubStats.__executeServer(opts));
var getGithubStats = createServerFn({ method: "GET" }).inputValidator((data) => data).handler(getGithubStats_createServerFn_handler, async ({ data }) => {
	const headers = {
		Accept: "application/vnd.github+json",
		"User-Agent": "portfolio-site"
	};
	try {
		const [reposRes, eventsRes] = await Promise.all([fetch(`https://api.github.com/users/${encodeURIComponent(data.user)}/repos?per_page=100&sort=pushed`, { headers }), fetch(`https://api.github.com/users/${encodeURIComponent(data.user)}/events/public?per_page=100`, { headers })]);
		if (!reposRes.ok) {
			console.error(`GitHub repos request failed [${reposRes.status}]`);
			return EMPTY;
		}
		const owned = (await reposRes.json()).filter((r) => !r.fork && !r.archived);
		const pinnedNames = (data.pinned ?? []).map((n) => n.toLowerCase());
		const repos = owned.map((r) => ({
			name: r.name,
			description: r.description,
			url: r.html_url,
			homepage: r.homepage,
			language: r.language,
			stars: r.stargazers_count,
			forks: r.forks_count,
			pushedAt: r.pushed_at,
			createdAt: r.created_at,
			topics: r.topics ?? [],
			pinned: pinnedNames.includes(r.name.toLowerCase())
		}));
		const pinned = repos.filter((r) => r.pinned).sort((a, b) => pinnedNames.indexOf(a.name.toLowerCase()) - pinnedNames.indexOf(b.name.toLowerCase()));
		const latest = [...repos].sort((a, b) => Date.parse(b.pushedAt) - Date.parse(a.pushedAt)).slice(0, 6);
		const counts = /* @__PURE__ */ new Map();
		for (const repo of repos) {
			if (!repo.language) continue;
			counts.set(repo.language, (counts.get(repo.language) ?? 0) + 1);
		}
		const total = [...counts.values()].reduce((sum, n) => sum + n, 0);
		const languages = [...counts.entries()].map(([name, count]) => ({
			name,
			count,
			percent: total ? Math.round(count / total * 1e3) / 10 : 0
		})).sort((a, b) => b.count - a.count);
		let activity = [];
		let commits = [];
		let days = [];
		if (eventsRes.ok) {
			const rawEvents = await eventsRes.json();
			days = buildDays(rawEvents);
			activity = rawEvents.slice(0, 8).map((event) => ({
				id: event.id,
				type: event.type,
				repo: event.repo.name.split("/").slice(-1)[0],
				repoUrl: `https://github.com/${event.repo.name}`,
				createdAt: event.created_at,
				detail: describeEvent(event)
			}));
			commits = rawEvents.filter((event) => event.type === "PushEvent").flatMap((event) => (event.payload?.commits ?? []).map((commit) => ({
				id: `${event.id}-${commit.sha}`,
				message: commit.message.split("\n")[0],
				repo: event.repo.name.split("/").slice(-1)[0],
				url: `https://github.com/${event.repo.name}/commit/${commit.sha}`,
				createdAt: event.created_at
			}))).slice(0, 8);
		} else days = buildDays([]);
		return {
			available: true,
			publicRepos: repos.length,
			totalStars: repos.reduce((sum, r) => sum + r.stars, 0),
			totalForks: repos.reduce((sum, r) => sum + r.forks, 0),
			pinned,
			latest,
			repos: [...repos].sort((a, b) => a.name.localeCompare(b.name)),
			languages,
			activity,
			commits,
			days
		};
	} catch (error) {
		console.error("GitHub request failed", error);
		return EMPTY;
	}
});
//#endregion
export { getGithubStats_createServerFn_handler };
